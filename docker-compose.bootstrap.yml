# docker-compose.bootstrap.yml
# create initial django environment 
#

services:
    django:
        build: 
            context: app
            dockerfile: ../docker/app/Dockerfile
        volumes:
            - ./app/:/usr/src/app/
        env_file:
            - ./.env.dev
